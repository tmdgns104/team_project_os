services:
  project-os:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ACCESS_KEY: ${APP_ACCESS_KEY:-}
      PROJECT_OS_DB: /data/project_os.db
    volumes:
      - project_os_data:/data
    restart: unless-stopped

volumes:
  project_os_data:
